@model IEnumerable<KendoMatbaa.Models.AltUretimAsamalari>

@{
    ViewBag.Title = "Index";
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Yeni", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AltUretimAsamalariAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UretimAsamalariId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AltUretimAsamalariAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UretimAsamalariId)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.AltUretimAsamalariId }) |
            @Html.ActionLink("Detay", "Details", new { id=item.AltUretimAsamalariId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.AltUretimAsamalariId })
        </td>
    </tr>
}

</table>
