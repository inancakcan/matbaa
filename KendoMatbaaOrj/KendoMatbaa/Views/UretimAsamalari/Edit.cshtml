@model KendoMatbaa.Models.UretimAsamalari

@{
    ViewBag.Title = "Düzenleme";
}

<h2>Düzenleme</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>UretimAsamalari</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.UretimAsamalariId)

        <div class="form-group">
            @Html.LabelFor(model => model.UretimAsamasiAdi, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.UretimAsamasiAdi)
                @Html.ValidationMessageFor(model => model.UretimAsamasiAdi)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Kaydet" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Liste", "Index")
</div>
